server:
  port: 8081

spring:
  application:
    name: mcp-shopping-server
  ai:
    mcp:
      server:
        name: shopping-tools-server
        version: 1.0.0
        type: SYNC
        # Streamable HTTP is the default transport for webmvc

logging:
  level:
    com.example.mcpserver: DEBUG
    org.springframework.ai: DEBUG
